<div class="row">
    <div class="col-md-offset-3 col-md-6 well">
      <div class="text-center">
        <h2 style="margin-top: 0px">Facultades</h2>
      </div>
      <% if user_signed_in? && current_user.skill?("Editar facultades") %>
        <%= form_for @new_faculty do |f|%>
          <%= render partial: 'errors', locals: { errors: @new_faculty.errors.full_messages } %>
          <div class="col-md-8">
            <%= f.text_field :name, class: "form-control" %>
          </div>
          <div class="col-md-4">
            <%= f.submit "Agregar", class:"btn btn-success btn-block" %>
          </div>
        <% end %>
      <% end %>
      <%= form_for @presenter.filter, url: faculties_path, method: :get do |f| %>
        <div class="col-md-8 margen-5">
          <%= f.text_field :search, class: 'form-control' %>
        </div>
        <div class="col-md-4 margen-5">
          <%= f.submit 'Buscar', class: 'btn btn-info btn-block' %>
        </div>
      <% end %>
    </div>
  <div class="list-group">
    <% @presenter.faculties.each do |faculty| %>
      <div class="list-group-item col-md-offset-3 col-md-6">
        <%= render faculty %>
      </div>
    <% end %>
  </div>
</div>
